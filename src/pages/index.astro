---
import { getCollection } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard.astro";
import Link from "@components/Link.astro";
import { compareProjects, dateRange } from "@lib/utils";
import { SITE, HOME, SOCIALS } from "@consts";

const blog = (await getCollection("blog"))
  .filter(post => !post.data.draft)
  .sort((a, b) => {
    if (a.data.order !== undefined || b.data.order !== undefined) {
      return (a.data.order ?? Number.MAX_SAFE_INTEGER) - (b.data.order ?? Number.MAX_SAFE_INTEGER);
    }

    return b.data.date.valueOf() - a.data.date.valueOf();
  })
  .slice(0,SITE.NUM_POSTS_ON_HOMEPAGE);

const projects = (await getCollection("projects"))
  .filter(project => !project.data.draft)
  .sort(compareProjects)
  .slice(0,SITE.NUM_PROJECTS_ON_HOMEPAGE);

const allwork = (await getCollection("work"))
  .sort((a, b) => new Date(b.data.dateStart).valueOf() - new Date(a.data.dateStart).valueOf())
  .slice(0,SITE.NUM_WORKS_ON_HOMEPAGE);

const work = await Promise.all(
  allwork.map(async (item) => {
    const { Content } = await item.render();
    return { ...item, Content };
  })
);

---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <div class="space-y-16">
      <section class="space-y-6">
        <div class="animate">
          <h1 class="text-lg font-semibold text-black dark:text-white">
            Jin Choi
          </h1>
          <p class="font-semibold text-black dark:text-white">
            Data Scientist
          </p>
        </div>
        <article class="space-y-4">
          <p class="animate">
            Hi! I am a Data Scientist at State Farm, working on solving real-world problems through statistical modeling, machine learning, and ML engineering.  
          </p>
          <p class="animate">
             I have become increasingly interested in building end-to-end machine learning systems beyond just modeling itself. Lately, I’ve been spending time learning more about containerization, model serving APIs, orchestration tools, cloud infrastructure, and the engineering workflows behind training and deploying ML systems at scale. And as I continue working in the insurance domain, I am realizing there are so much to learn. I am currently working toward completing my CPCU certification (3/4 way done 🥳) to strenghten my business knowledge in the finance and insurance domain.
          </p>
        </article>
      </section>

      <hr class="animate border-black/10 dark:border-white/15" />

      <section class="animate space-y-3">
        <h2 class="font-semibold text-black dark:text-white">
          Certifications
        </h2>
        <ul class="space-y-1">
          <li>
            <Link
              href="https://www.credly.com/badges/cd40b52f-75fe-454d-a612-264301b4beba/public_url"
              external
            >
              AWS Certified Solutions Architect
            </Link>
          </li>
          <li>
            <Link
              href="https://www.credly.com/badges/3934f76d-6a13-49bc-9db7-998a67527ab6"
              external
            >
              AWS Certified Cloud Practitioner
            </Link>
          </li>
        </ul>
      </section>

      <hr class="animate border-black/10 dark:border-white/15" />

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h2 class="font-semibold text-black dark:text-white">
            Work
          </h2>
          <Link href="/work">
            See all work
          </Link>
        </div>
        <ul class="space-y-6">
          {work.map(entry => (
            <li>
              <div class="font-medium text-black dark:text-white">
                {entry.data.company}
              </div>
              <div class="text-sm text-black/50 dark:text-white/60">
                {entry.data.role}
              </div>
              <div class="text-sm text-black/50 dark:text-white/60">
                {dateRange(entry.data.dateStart, entry.data.dateEnd)}
              </div>
              <article class="mt-3 text-base">
                <entry.Content />
              </article>
            </li>
          ))}
        </ul>
      </section>

      <hr class="animate border-black/10 dark:border-white/15" />

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h2 class="font-semibold text-black dark:text-white">
            Recent projects
          </h2>
          <Link href="/projects">
            See all projects
          </Link>
        </div>
        <ul class="divide-y divide-black/10 dark:divide-white/15">
          {projects.map(project => (
            <li>
              <ArrowCard entry={project} />
            </li>
          ))}
        </ul>
      </section>

      <hr class="animate border-black/10 dark:border-white/15" />

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h2 class="font-semibold text-black dark:text-white">
            Blog posts
          </h2>
          <Link href="/blog">
            See all posts
          </Link>
        </div>
        <ul class="divide-y divide-black/10 dark:divide-white/15">
          {blog.map(post => (
            <li>
              <ArrowCard entry={post} />
            </li>
          ))}
        </ul>
      </section>

      <hr class="animate border-black/10 dark:border-white/15" />

      <section class="animate space-y-4">
        <h2 class="font-semibold text-black dark:text-white">
          Let's Connect!
        </h2>
        <article>
          <p>
            If you would like to speak more, feel free to reach out to me via any of the channels below.
          </p>
        </article>
        <ul class="flex flex-wrap gap-2">
          {SOCIALS.map(SOCIAL => (
            <li class="flex gap-x-2 text-nowrap">
              <Link href={SOCIAL.HREF} external aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}>
                {SOCIAL.NAME}
              </Link>
              {"/"}
            </li>
          ))}
          <li class="line-clamp-1">
            <Link href="mailto:jinchoi595@gmail.com" aria-label="Email Jin Choi">
              jinchoi595@gmail.com
            </Link>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>
